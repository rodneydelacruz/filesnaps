{
  "name": "filesnaps",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:worker\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:worker": "cd worker && npm run dev",
    "build": "cd frontend && npm run build",
    "deploy": "cd frontend && npm run build && cd ../worker && npm run deploy",
    "install:all": "cd frontend && npm install && cd ../worker && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
